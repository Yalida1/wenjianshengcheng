# Project Instructions

本仓库用于建设可实际运行和交付的“项目文件链式生成平台”。

开始任何代码修改前，必须完整阅读仓库根目录：

- MASTER_BUILD_PROMPT.md

`MASTER_BUILD_PROMPT.md` 是当前项目的总体产品需求、业务规则、
技术架构、文档生成、格式标准、测试和交付要求。

## 一、产品主链路

唯一核心业务链路：

需求说明 / 项目建议书
→ 可研报告
→ 招标文件
→ 合同

每个下游阶段都支持：

1. 使用平台上一阶段已经定稿的文件；
2. 上传用户已有文件。

本期不建设独立的：

- 评标业务流程；
- 中标业务流程；
- 评委管理；
- 供应商管理；
- 履约台账；
- 自动发布。

招标文件模板内部可以包含评标办法等章节，
但不得据此建设独立评标业务模块。

## 二、现有前端

当前仓库包含 Figma Make 生成的：

- React 19；
- TypeScript；
- Vite；
- Tailwind CSS；
- Hash 页面状态；
- MockStore 模拟数据；
- 项目、可研、招标、合同及管理后台页面。

必须保留现有前端的主要视觉风格、页面布局和业务流程。

允许重构代码结构、状态管理和路由，
但不得整体推翻或重新设计现有前端。

Mock 数据必须逐步替换为真实 API 和服务端数据。

## 三、关键业务规则

1. 禁止无来源编造金额、主体、日期、范围、数量、期限、税率和付款信息。
2. AI 建议只能作为候选值，不能直接成为正式值。
3. 所有 P0 阻断级字段必须有来源证据或人工确认。
4. P0 字段存在 missing、conflict、invalid 或 ai_suggested 状态时，不得定稿。
5. 可研总投资不等于招标预算。
6. 可研总投资不等于最终合同金额。
7. 招标预算和最高限价不等于最终合同金额。
8. 项目总建设周期不等于单份合同履行期限。
9. 项目全部建设范围不等于单份采购范围或合同范围。
10. 上游文件或模板更新时，不得静默覆盖下游文件。
11. 已定稿文件不得直接覆盖，只能创建新修订版本。
12. 客户正式模板优先于平台通用格式。

## 四、工程要求

1. 不得只输出计划而不实施。
2. 不得用 TODO、空白页面或“功能待开发”代替实现。
3. 不得使用假接口长期冒充正式功能。
4. 不得将密钥、密码或客户敏感材料提交到 Git。
5. 所有数据修改必须考虑事务、幂等、revision 和并发冲突。
6. 所有新增 API 必须配套 Schema、权限、错误处理和测试。
7. 所有异步任务必须支持状态查询、超时、重试和失败恢复。
8. 所有正式文档必须通过格式、字段、结构和一致性校验。
9. 前后端类型和 OpenAPI 契约必须保持一致。
10. 实际运行 lint、typecheck、build、单元测试、集成测试和 E2E。

## 五、交付门禁

最终必须提供：

- README.md
- .env.example
- docker-compose.yml
- Makefile
- 数据库迁移
- API 文档
- 部署文档
- 运维文档
- 安全文档
- 测试文档
- DELIVERY_REPORT.md
- TEST_RESULTS.md
- KNOWN_LIMITATIONS.md
- Golden Case 生成文件及校验报告

最终必须运行：

```bash
make verify