# 已知限制

- 待编制文件误识别与按文件动态字段采集修复（分支 `fix/pending-doc-misidentify-and-scoped-fields`）已用合成样本与专项单测覆盖审查约定的 A–L 回归；仓库内未定位到原审查 zip 中的实体「北京地铁…标准可研…」DOCX，故不以该实体文件的端到端跑通作为门禁结论。历史库清理脚本默认 dry-run，生产 `--apply` 前须备份。详见 `docs/FIX_PENDING_DOC_MISIDENTIFY.md` 与 `artifacts/pending-doc-misidentify-before-after.md`。本项不承诺未经多份独立样本测得的整体识别精确率。
- 确定性 Demo Provider 用于自动验收。DeepSeek 兼容接口已完成无业务数据的模型列表、JSON 响应和平台 Provider 连通验证，但未使用客户真实成品文件评估章节/变量召回率，也不代表生产数据合规审查已经完成。
- 成品文件模板反向提取当前仅支持 DOCX；PDF、扫描件和 OCR 内容暂不作为可保留原始 Word 版式的模板源。提取结果是候选草稿，仍需人工校准和手工发布。
- 模板中心已内置国家正式文本的权威来源索引和依据正式大纲制作的生成适配版，但没有把平台适配版冒充主管部门原文；正式项目仍须核对官方最新文本、专项行业要求和客户批准模板。客户模板与历史人工样本尚未提供。
- 本次设备采购 Golden Case 使用的是“平台参考模板”，不是经过客户授权、版本锁定的官方标准条款全文，因此不能据此声称严格符合某一主管部门官方模板；客户正式模板上传、版本、来源、适用范围和哈希锁定能力已经接入，但仍需客户提供合法模板后再做项目级合规验收。
- Demo 容器使用开源 Noto CJK 字体；客户授权字体和目标办公软件环境尚未验收。
- DOCX 是可编辑文件，不同 Word/LibreOffice 版本、字体替换和打印机度量可能导致重新分页。本次 DOCX/PDF 在交付基线 LibreOffice 24.2 中均为 27 页且逐页图像一致；另行用 LibreOffice 25.2 打开时得到 25 页，因此生产环境应固定转换镜像，客户端编辑后须重新生成平台正式 PDF。
- OCR 仅保留扩展和 `needs_ocr` 状态，未集成生产 OCR 引擎；病毒扫描保留扩展接口，未连接企业病毒扫描服务。
- 本期不含独立评标、中标、评委、供应商、履约台账和自动发布模块。
- 超长可研已采用分章节可恢复任务模型，但 50—100 页的客户真实材料容量、格式和并发压力仍需在生产规格环境验证。
- 采购方案分析已实现全文分块、结构化 Schema、引用校验和确定性测试 Provider。当前仓库没有客户真实可研、经批准采购方案或可用于生产的模型密钥，因此真实材料的语义召回率、候选划分质量和法规适用性仍未验收；Demo/Test 结果在页面明确标识，不能冒充真实分析。
- 采购规则集可以记录适用主体、地区、资金性质、来源版本和生效期，但平台不会仅凭“国企”等单一信息自动认定制度适用，也没有内置全国统一金额门槛。正式使用前需由业务/法务提供并批准适用规则及客户模板。
- 外部 SSO、业务系统接口、生产域名/证书、WAF/SIEM、灾备设施和客户侧安全测试依赖外部资源。
