node_modules/
.pnpm-store/
dist/
build/
.cache/
.env*
!.env.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
.env
!.env.example
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.audit-cache/
htmlcov/
.coverage
data/
outputs/runtime/
tmp/
playwright-report/
test-results/
.codex-git-checkpoint/

# Local debug / scratch outputs (do not publish)
artifacts/_*
artifacts/_e2e_fail/
artifacts/_e2e_fail2/
backend/tests/_debug_injection.py
