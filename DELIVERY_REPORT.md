# 项目文件链式生成平台交付报告

交付验收日期：2026-09-09（本次增量）；完整平台基线记录保留于 2026-09-08。

交付状态：仓库规定的完整 `make verify` 已实际执行并以退出码 0 完成。

## 交付范围

本次交付将原有 Figma Make React 原型扩展为可运行的前后端系统，业务范围严格限定为：

需求说明 / 项目建议书 → 可行性研究报告 → 采购方案分析与确认 → 一份或多份招标文件 → 按已确认采购范围生成合同草稿。

每个下游阶段都支持使用上一阶段已定稿文件，或上传用户已有文件。本期没有建设独立评标、中标、评委、供应商、履约台账和自动发布模块。

## 已交付能力

- React 19、TypeScript、Vite、Tailwind CSS 前端，保留原有深蓝、白、浅灰的主要视觉与工作流结构。
- FastAPI、SQLAlchemy、Alembic、PostgreSQL 后端，包含 61 张表和 12 个顺序迁移。
- HttpOnly 会话、CSRF、Argon2、五类角色、权限、组织/项目数据范围和审计日志。
- 文件上传、不可变版本、SHA-256、类型校验、解析任务、来源证据、字段确认、冲突和快照。
- P0 定稿阻断、金额和周期禁止错误映射、合同付款比例/金额/触发条件校验。
- 可研全文分块采购分析、覆盖率记录、证据定位、冲突/解析缺口、建设内容去重、采购包与主招标文件一对多分组，以及程序计算的文件份数。
- 推荐/备选采购方案版本、用户合并拆分与调包、Decimal 预算分配核对、可配置采购规则、revision 并发保护、确认快照和来源变化待复核。
- 每份主招标文件独立模板匹配、单份/批量幂等生成、逐份状态和失败重试；合同可从多标包招标文件中选择部分采购包继承范围，且不把预算或限价当作成交价。
- DOCX 模板源文件、不可覆盖版本、预检、变量、章节和版式配置。
- 四类模板来源治理：9 份国家正式文本权威来源索引、9 份正式大纲适配模板、8 份平台参考模板，以及客户/行业/地区“其他正式模板”归档入口；官方原文禁止进入生成列表。
- 成品 DOCX 的“程序解析 + 小模型理解 + 人工确认”模板反向提取：外部处理授权、异步状态/重试、逐块证据校验、候选审核、脱敏候选 DOCX 和草稿确认均已接入；不自动发布。
- 项目列表提供管理员项目删除：项目编号二次确认、当前修订冲突保护、运行中任务阻断、完整数据链事务删除、对象文件清理和删除审计均已接入。
- 项目、阶段、文件、生成、校验和模板等常见状态统一显示为中文，保留原始状态码用于接口和诊断。
- Demo 与 OpenAI-compatible Provider、Celery 异步分步生成、事件、重试、取消和恢复。
- 解析与生成等待界面提供统一动画进度、当前处理步骤、真实完成数量和结果说明；失败时保留状态与恢复入口，不伪造百分比或完成结果。
- 文档生成目录支持每个小章节独立选择、父章批选后排除子章，以及仅按最终勾选范围生成。目录采用轻量行选中样式，右侧固定预览当前点击的小章节正文。
- 文档确认按当前招标文件归属展示，未生成正文时显示空状态；切换多份招标文件不会串用其他文件或旧的未绑定草稿正文。
- 文档块编辑、审阅、批注、对比、校验、定稿、修订和 DOCX/PDF/XLSX 导出。
- 招标文件“正文审阅 / 正式排版预览”双视图：正文审阅保留目录、正文和 AI 三栏；正式排版预览直接显示服务端 PDF 导出结果，并记录生成中、成功、失败、最后生成时间、文档版本和修订号。
- 设备采购招标文件已扩展为封面、可更新目录、六章、50 余个小节和 20 余张业务表格；一级章节换页，表格跨页重复表头，DOCX 保持可编辑，PDF 与预览复用同一 DOCX 转换链路。
- 定稿状态、审阅完成与正式定稿分离；P0/P1 来源和人工确认、模板适用性、章节、金额、日期、范围、评分与合同条款校验均进入定稿门禁，已定稿文档只读并通过新修订继续修改。
- Docker Compose 的 PostgreSQL、Redis、MinIO、API、worker、web 六个运行服务，以及独立 verify 验收容器。
- Golden Case 四阶段文件、合同预草案、字段追溯表、校验报告与格式合规报告。
- 运维、备份恢复、安全、模板制作、用户与管理员说明等交付文档。

## 验收口径

唯一整体通过口径是仓库根目录的字面量 `make verify` 返回退出码 0。该命令串行执行环境预检、前后端静态检查与测试、空库迁移、OpenAPI 契约、备份恢复、真实浏览器 E2E、Golden Case、依赖/密钥安全扫描和最终交付件门禁。每条命令的真实结果记录在 `TEST_RESULTS.md`。

本次最终执行结果为：前端 38/38、后端 88/88、后端语句覆盖率 85.18%、浏览器 E2E 6/6、Golden Case 313/313、12 个迁移在全新数据库生成 61 张表、Python/Node 已知漏洞 0，最终交付件门禁通过。完整日志为 `artifacts/chapter-selection-delivery-verify.log`。设备采购 E2E 实际完成来源上传、采购属性识别、六章模板匹配、字段确认、正文审阅、定稿校验、定稿、DOCX/PDF 导出和同源排版预览。

本次增量额外覆盖单个小章节的精确生成与正文联动、未选正文保留、未生成文件的确认空状态、多份招标文件隔离，以及真实计数的动画进度反馈。页面已更新到本地 `http://localhost:18080`；没有因验收重写用户当前项目正文。依赖审计和旧 UI 断言的失败、修复与重跑过程详见 `TEST_RESULTS.md`。

## 2026-09-10 待编制文件误识别与按文件动态字段采集

分支：`fix/pending-doc-misidentify-and-scoped-fields`。未重新设计平台布局，未新增独立评标模块。

已落地：规则通道 v2（汇总段/profile 假阳/文件清单表）、融合与 ensure-default 取消静默一包一套、StagePage 改为人工确认分组、表格角色抽取与 scoped 字段、估算不灌预算、别名增量迁移、增量补采、适用字段门禁与历史 heal 脚本。

专项实测：pytest **52 passed**，Vitest **40 passed**，`tsc --noEmit` 通过；OpenAPI/`schema.d.ts` 已同步。完整 `make verify` 未在本专项收口重跑。实体示范可研 DOCX 未入库，对照见 `artifacts/pending-doc-misidentify-before-after.md`；迁移回滚见 `docs/FIX_PENDING_DOC_MISIDENTIFY.md`。

## 2026-09-08 招标文件专项验收（历史记录）

- 实际浏览器项目 `BTA-UI-1788868144555` 匹配“设备采购招标文件平台参考模板”，采购类别为“设备采购”，定稿版本为 V1 / R140。
- 页面正文共六章，关键小节由多段项目化条款和结构化表格组成；浏览器文本检查未发现重复句号和错误截止时间拼接。
- Golden 招标文件 DOCX 与 PDF 均在 LibreOffice 24.2 验收运行时渲染为 27 张 PNG，逐页文件哈希差异为 0；封面、两页目录、六章、前附表、评分跨页表、合同文本、采购清单及投标格式附件均完成视觉检查。
- 字段来源清单作为独立 XLSX 交付，不默认附入对外招标文件正文。

## 外部依赖边界

客户正式模板、历史人工样本、生产 OCR、企业病毒扫描、SSO、业务系统接口、生产域名与证书、客户授权字体、WAF/SIEM 和灾备设施未随输入提供。DeepSeek 仅完成无业务数据连通和结构化响应验证，不声称客户材料准确率或生产数据合规已经验收。具体边界见 `KNOWN_LIMITATIONS.md`。
